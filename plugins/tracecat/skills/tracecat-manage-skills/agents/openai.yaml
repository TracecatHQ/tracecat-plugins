interface:
  display_name: "Manage Tracecat Skills"
  short_description: "Upload, publish, and re-bind Tracecat skills"
  default_prompt: "Use $tracecat-manage-skills to upload a local skill directory to Tracecat, publish it, and re-bind the agent presets that use it."
